#include "holberton.h"

char *_strncat(char *dest, char *src, int n)
{

	int i;
        while (*dest)
        {
                dest++;
        }
        *deset = _putchar("");
        for (*dest; i <= n;i++)


       return (dest);
}
