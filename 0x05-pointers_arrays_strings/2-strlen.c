#include "holberton.h"

/**
 * _strlen - Write a function that returns the length of a string.
 *
 * @s: pointer to a char
 */
int _strlen(char *s)
{
	int i;

	while (*s != NULL)
		i++;
	return (i);
}
