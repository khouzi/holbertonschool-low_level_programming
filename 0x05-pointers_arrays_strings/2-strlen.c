#include "holberton.h"

/**
 * int _strlen - Write a function that returns the length of a string.
 * @s: pointer to a char
 *
 * Return: The length of @str.
 */
int _strlen(char *s)
{
	int i;

	i = ;
	while (*s++)
		i++;
	return (i);
}
i 
